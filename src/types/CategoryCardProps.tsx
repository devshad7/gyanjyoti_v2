export type CategoryCardProps = {
  icon: React.ReactNode;
  id: string | number;
  description:string;
  hours:string | number;
  title: string;
  url: string;
  count: string | number;
  bgColor?: string;
  textColor?: string;
};
